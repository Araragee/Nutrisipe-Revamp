export enum PostCategory {
  Recipe = 'recipe',
  MealPhoto = 'meal_photo',
  NutritionTip = 'nutrition_tip',
  CookingTechnique = 'cooking_technique',
}

export enum ActivityType {
  Follow = 'follow',
  Like = 'like',
  Post = 'post',
}
